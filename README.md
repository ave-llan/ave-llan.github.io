Personal site built with [Jekyll](https://jekyllrb.com/).


# To build
Install `jekyll` and `bundler` if needed:
```
gem install jekyll bundler
```

Then, build and start the preview server:
```
jekyll serve
```

View the site at `localhost:4000`.